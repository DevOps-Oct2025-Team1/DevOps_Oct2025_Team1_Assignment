steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/auth-service:latest', '.']
    id: 'Build Auth Service'

images:
  - '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/auth-service:latest'

substitutions:
  _REGION: 'us-central1'
  _REPOSITORY: 'devops-test-repo'
